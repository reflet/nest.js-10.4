services:
  app:
    image: 'nest.js-10.4:create'
    build:
      context: ./
      dockerfile: ./docker/Dockerfile
      target: create
    tty: true
    volumes:
      - ./src:/usr/src/app
    ports:
      - "3000:3000"
