services:
  app:
    image: 'nest.js:production'
    build:
      context: ./
      dockerfile: ./docker/Dockerfile
      target: production
    ports:
      - "3000:3000"
    entrypoint: node dist/main.js
